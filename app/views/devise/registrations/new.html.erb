<div class="container">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
      <div class="text-center">
        <% if params[:plan] == '2' %>
          <h1>Pro Account</h1>
          <p>Sign up for the pro account for $1/month and get access to the community and contact information!</p>
          <div class="alert-disclaimer" role="alert">
            <h3>We use <a href="https://stripe.com" class="alert-disclaimer-link">Stripe</a> to process credit card transactions and 
            are currently using Stripe's test mode in which no payments will be processed. You can read 
            more about Stripe's testing <a href="https://stripe.com/docs/testing" class="alert-disclaimer-link">here</a></h3>
          </div>
        <% else %>
          <h1>Basic Account</h1>
          <p>Sign up for free and get basic access to the community.</p>
        <% end %>
      </div>
      <div class="well">
        <h2>Sign Up</h2>
        <% if params[:plan] == '2' %>
          <%= render "pro_form" %>
        <% else %>
          <%= render "basic_form" %>
        <% end %>
        
        <%= render "devise/shared/links" %>
      </div>
    </div>
  </div>
</div>